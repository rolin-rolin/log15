{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and prompt window",
  "windows": ["main", "prompt"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
